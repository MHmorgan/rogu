Setup:

	python3 -m venv venv
	source venv/bin/activate
	make update

Build zipapp:

	make build

Run tests (the three are equivalent; requires setup):

	./test all
	make test
	make

Run a specific test(s):

	./test [test-name...]

Test names:

	ugor
	file
	archive
